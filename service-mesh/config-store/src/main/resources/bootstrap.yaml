spring:
  application:
    name: config-store
  config:
    import:
    - classpath:cloud-config-server.yaml
    - classpath:key-store.yaml
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
    group:
      "dev" : "native, local"
      "stage" : "remote, stage"