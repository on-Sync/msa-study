spring:
  application:
    name: discovery
  config:
    import:
    - classpath:cloud-config-client.yaml
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
    group:
      "dev" : "local"
      "stage" : "stage"