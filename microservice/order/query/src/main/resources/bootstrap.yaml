spring:
  application:
    name: order-service-query
  config:
    import:
    - classpath:cloud-config-client.yaml
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
    group:
      "dev" : "local"
      "stage" : "stage"